# GraphQL schema
#
# https://gqlgen.com/getting-started/

enum NodeAccess {
  public
  restricted
  private
}

enum PostType {
  text
  media
  link
}

type Post {
  id: ID!
  title: String!
  link: String
  delta: String
}

input NewPost {
  type: PostType!
  title: String!
  link: String
  delta: String
}

type Node {
  id: ID!
  name: String!
  tags: [String]
  access: NodeAccess!
  nsfw: Boolean!
}

input NewNode {
  name: String!
  tags: [String]
  access: NodeAccess!
  nsfw: Boolean!
}

type Query {
  posts: [Post!]!
  nodes: [Node!]!
}

type Mutation {
  createPost(newPost: NewPost!): Post!
  createNode(newNode: NewNode!): Node!
}
