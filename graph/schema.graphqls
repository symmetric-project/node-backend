# GraphQL schema
#
# https://gqlgen.com/getting-started/

enum NodeAccess {
  PUBLIC
  RESTRICTED
  PRIVATE
}

type Post {
  id: ID!
  name: String!
  link: String
  delta: String
}

input NewPost {
  name: String!
  link: String
  delta: String
}

type Node {
  id: ID!
  name: String!
  tags: [String]!
  access: NodeAccess!
  nsfw: Boolean!
}

input NewNode {
  name: String!
  tags: [String]!
  access: NodeAccess!
  nsfw: Boolean!
}

type Query {
  posts: [Post!]!
  nodes: [Node!]!
}

type Mutation {
  createPost(input: NewPost!): Post!
  createNode(input: NewNode!): Node!
}
